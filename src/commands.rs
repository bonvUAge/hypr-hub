// 🦀 Модуль для выполнения системных команд
// Здесь мы работаем с запуском внешних процессов

use std::io::Result;
use std::process::Command;

// 🦀 Функция для выполнения команды через shell
// В JavaScript это было бы что-то вроде child_process.exec()
pub fn execute_command(command_str: &str) -> Result<()> {
    println!("🦀 Executing: {}", command_str);

    // 🦀 Command::new("sh") - создаём новый процесс оболочки
    // .arg("-c") - передаём аргумент "-c" (выполнить команду)
    // .arg(command_str) - передаём саму команду
    // .status()? - запускаем и ждём завершения
    //
    // Оператор ? - это syntactic sugar для обработки ошибок
    // Если Result это Err, функция немедленно вернёт ошибку
    // В JavaScript это похоже на: if (err) throw err;
    Command::new("sh")
        .arg("-c")
        .arg(command_str)
        .status()?;

    Ok(())
}

// 🦀 В будущем здесь можно добавить:
// - execute_command_with_output() - для команд с выводом
// - execute_command_async() - для асинхронного выполнения
// - validate_command() - для проверки команд перед запуском
